
---

#### How to install this build

- **Chrome / Edge / Brave / Vivaldi**: `uBlockVanced-%version%.chromium.zip` — extract, then `chrome://extensions` → Developer mode → **Load unpacked**. Or `uBlockVanced-%version%.chromium.crx` — drag onto `chrome://extensions` (Chrome may block unlisted CRX installs; the zip always works).
- **Firefox (desktop and Android)**: `uBlockVanced-%version%.firefox.xpi` — unsigned, so use `about:debugging` → **This Firefox** → **Load Temporary Add-on**, or set `xpinstall.signatures.required=false` in Firefox Developer Edition / Nightly / ESR for a permanent install.
- **Opera**: `uBlockVanced-%version%.opera.zip` — same steps as Chrome via `opera://extensions`.
- **Thunderbird**: `uBlockVanced-%version%.thunderbird.xpi` — Add-ons Manager → gear icon → **Install Add-on From File**.

uBlockVanced is a fork of [uBlock Origin](https://github.com/gorhill/uBlock) by Raymond Hill. Upstream ad-blocking functionality is unchanged.
